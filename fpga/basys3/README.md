# Basys3 / Artix-7 规划

状态：**planned / not implemented**。

- target board：Basys3；
- FPGA family：Artix-7；
- 后续关注：BRAM 权重存储、membrane/state storage、DSP/adder reuse、temporal
  controller、UART/PC input、board-level validation。

本目录当前不包含 RTL、`.xpr`、`.xdc`、自动生成物或 bitstream。硬件目标将在正式 SNN
champion、数值位宽和资源预算冻结后再建立。
