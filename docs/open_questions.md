# 待确认问题

> 本文档按"已确认结论 → A 轨阻塞 → B 轨阻塞 → C 轨研究 → 不再询问的问题"分类。阻塞性问题必须在 A1/B1 阶段解决。

---

## ✅ 已确认结论

| 问题 | 结论 | 来源 |
|------|------|------|
| 当前有什么硬件？ | 4×4 光学器件阵列，16 单元 | 项目负责人 |
| 外围电路是否需要？ | **需要**——用于传感器读出和器件表征 | 文档重构 M0 确认 |
| 外围电路用途？ | 传感采集，**不是计算阵列外围** | 文档重构 M0 确认 |
| 是否需要多档采样和长时监测？ | **需要**——系统必须具备这些能力 | 文档重构 M0 确认 |
| FPGA 做什么？ | 部署 28×28 数字 CNN 前向推理 | 文档重构 M0 确认 |
| 4×4 和 28×28 的关系？ | 两部分**不直接连接**，是独立主线 | 文档重构 M0 确认 |
| 数据集是否 MNIST？ | 是——28×28 手写数字 | 已确认 |
| 是否 CNN？ | 是——第一版基线为 CNN | ADR 002 |
| 是否 SNN？ | 不是——当前阶段不做 | ADR 002 + [P4] |
| 是否计算阵列？ | 不是——外围电路用于传感器读出 | 文档重构 M0 确认 |
| 参数能否先用课题组论文？ | 可以——后续用实测替换 | 已确认 |
| 外部论文 P3 是否有参考价值？ | 是——差分权重和参数映射方法 | 论文分析 |
| 是否需要 Adam 基线？ | **必须保留**——普通 CNN 是 FPGA 推理基线 | 文档重构 M0 确认 |
| 二维半导体是否等于宽禁带？ | 不是——WSe₂ 带隙 ~1.2 eV | [P2] |
| 器件叫"突触"是否必须用 SNN？ | 不是——指模拟生物突触可塑性，非算法约束 | ADR 002 |

---

## 🔴 A 轨阻塞问题（必须优先解决）

> 以下问题须在 A1 阶段向老师或研究生确认。**全部未完成前不得通电。**

| # | 问题 | 影响设计 | 优先级 |
|---|------|---------|--------|
| AQ1 | **官方 pinout**（引脚号 → 功能映射） | 所有电路设计的前提 | 🔴 最高 |
| AQ2 | **封装朝向**（1 号脚位置、正反面） | 连接方向确认 | 🔴 最高 |
| AQ3 | **端口方向**（每端口输入/输出/双向） | 电路连接方式 | 🔴 最高 |
| AQ4 | **公共端/参考端**（共享/独立） | 参考点选择 | 🔴 最高 |
| AQ5 | **阵列拓扑**（独立/交叉/晶体管/复用） | 通道方案和 MUX 需求 | 🔴 高 |
| AQ6 | **推荐偏置条件**（每端口电压/电流） | 偏置电路设计 | 🔴 最高 |
| AQ7 | **绝对最大额定值**（Vmax/Imax/Pmax） | 安全保护设计 | 🔴 最高 |
| AQ8 | **每单元端口数**（2 端/3 端/4 端） | 通道数和 MUX 配置 | 🔴 高 |
| AQ9 | **输出信号类型**（电流/电压/电荷） | 读出电路拓扑 | 🔴 高 |
| AQ10 | **输出范围、暗电流、噪声水平** | 动态范围和 ADC 需求 | 🔴 高 |
| AQ11 | **读偏置条件**（读出时偏置电压/电流） | 读出电路设计 | 🔴 高 |
| AQ12 | **最快有效响应时间**（上升/下降） | 快速瞬态采样率派生 | 🔴 高 |
| AQ13 | **最长衰减/恢复时间** | 长时监测窗口规划 | 🔴 高 |
| AQ14 | **是否必须 16 路同时采样** | MUX 可行性 | 🔴 高 |
| AQ15 | **能否使用 MUX 轮询** | 通道方案简化可能性 | 🟡 中 |
| AQ16 | **光源条件**（波长/功率/光斑/耦合） | 光学实验设计 | 🟡 中 |
| AQ17 | **效果验证标准**（什么算"正常"响应） | 测试判定依据 | 🟡 中 |
| AQ18 | **是否与 P2 同材料/同结构/同批次** | 论文参数可用性 | 🟡 中 |
| AQ19 | **已知的失效模式和敏感性** | 安全预防 | 🔴 高 |

---

## 🟡 B 轨阻塞问题

> 以下问题须在 B1 阶段确定。B 轨可先启动 Python 训练，但具体实现依赖这些决策。

| # | 问题 | 影响设计 | 预计解决 |
|---|------|---------|---------|
| BQ1 | **具体 FPGA 型号**（AC620 子型号） | LE/M9K/DSP 上限 | M0 |
| BQ2 | **板载存储资源**（是否外挂 SRAM/SDRAM） | 权重存储方案 | M0 |
| BQ3 | **CNN 网络结构**（层数、核大小、通道数） | 全部实现 | M0–M1 |
| BQ4 | **定点位宽和策略**（INT8/INT16/自定义） | 量化方案 | M1 |
| BQ5 | **图像输入方式**（串口/ROM/SD卡/其他） | 输入接口 | M1 |
| BQ6 | **权重加载方式**（随代码编译/动态加载） | 存储方案 | M1 |
| BQ7 | **结果输出方式**（数码管/UART/串口） | 输出接口 | M1 |
| BQ8 | **延迟/资源/准确率目标** | 架构权衡 | M1 |
| BQ9 | **是否必须上板运行**（或仿真验证即可） | 验收条件 | M0 |

---

## 📋 C 轨研究问题

> 以下问题为器件曲线建模的研究探索，不阻塞 A/B 交付。

| # | 问题 | 阶段 |
|---|------|------|
| CQ1 | 器件有几类曲线需要建模？（光响应 vs 权重更新 vs 其他） | C1 |
| CQ2 | P2 的 LTP/LTD 曲线能否用数学模型拟合？参数？ | C1 |
| CQ3 | 如何将梯度下降中的更新量映射为器件脉冲数/电导状态？ | C2 |
| CQ4 | 差分权重 `G+ − G−` 如何在 CNN 中表示？（不仅是单层感知机） | C3 |
| CQ5 | 器件非线性和有限状态如何影响训练收敛和最终精度？ | C3 |
| CQ6 | 器件噪声（读噪声、涨落、漂移）应如何在训练中建模？ | C3 |
| CQ7 | 如何区分训练期噪声和推理期噪声的影响？ | C3 |
| CQ8 | 论文参数与未来实测参数的差异会对结果产生多大影响？ | C4 |
| CQ9 | 自定义优化器的评价标准是什么？（精度/资源/收敛速度？） | C3 |
| CQ10 | 当参数更新受到器件非线性约束时，模型性能如何变化？ | C3 |

---

## ❌ 不再询问的问题

以下问题已在文档重构中确认或不再适应当前范围：

| ~~旧问题~~ | ~~原因~~ | 新状态 |
|-----------|---------|--------|
| ~~外围电路是否需要？~~ | 已确认需要 | → 已确认（A 组需求） |
| ~~4×4 是否直接输入 FPGA？~~ | 明确不要求连接 | → 已确认（不连接） |
| ~~网络是否处理 4×4 时间序列？~~ | 28×28 CNN 不处理 4×4 数据 | → 已确认 |
| ~~数据集是否 MNIST？~~ | 已确认为 28×28 手写数字 | → 已确认 |
| ~~是否 CNN？~~ | ADR 002 已决定 | → 已确认 |
| ~~是否计算阵列？~~ | 外围电路用于传感器读出 | → 已确认 |
| ~~是否 SNN？~~ | ADR 002 已排除 | → 已确认 |
| ~~FPGA 输入形式待确认？~~ | 数字源，不来自 4×4 | → BQ5 细化 |
| ~~Vivado/FINN/PYNQ？~~ | 工具链为 Quartus | → 已确认 |

---

## 问题跟踪说明

- 阻塞问题解决后，对应答案写入 [interface_specification.md](interface_specification.md)
- A 轨阻塞全部解决前不得通电
- B 轨可先于 A 轨启动，不受器件规格影响
- C 轨问题在对应阶段启动前重新评估
